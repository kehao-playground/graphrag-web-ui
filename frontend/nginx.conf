server {
  listen 80;
  location /api/ {
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;  # api 以此取得真實客戶端 IP(登入速率限制)
    proxy_buffering off;            # SSE(spec §8.1)
    proxy_read_timeout 3600s;
  }
  location / { root /usr/share/nginx/html; try_files $uri /index.html; }
}
